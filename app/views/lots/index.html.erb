<div class="container-fluid">
     <div class="banner" style="background-color: #59ABE3">
       <div class="container">
       <h3><strong><%= link_to "Blog", actualitys_path %></strong> <strong><%= link_to "Contact", contact_path %></strong></h3>
        <h2>Trouvez votre chez-vous </h2>
  <div class="search">
          <%= form_tag lots_path, method: :get do %>
            <%= text_field_tag :query, params[:query],
             class: "form-control", id: "city", placeholder: "Où voulez-vous vivre?"  %>
              <%= text_field_tag :query, params[:query],
             class: "form-control", id: "price", placeholder: "Prix max"  %>
           <%= submit_tag "Search", class: "btn btn-primary" %>
         <% end %>
                  </div>
        <h1> <strong><%= link_to "Achat", propertys_path %></strong> <strong><%= link_to "location", locations_path %></strong> <strong><%= link_to "terrain", lots_path, {:style=>'color: rgba(255,255,255, 0.8);'} %></strong> estimation </h1>

      </div>
    </div>

    <% @lots.each do |l| %>
  <div class="card-product">
    <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" />
     <div class="card-product-infos">
      <h2><%= link_to l.name,  lot_path(l) %></h2>
      <p> <%= l.city %> </p>
    </div>
  </div>
   <% end %>
</div>

